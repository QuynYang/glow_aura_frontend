# Sao chép thành .env (dev local) hoặc dùng .env.production (đã có URL Railway)
# Có thể bỏ hậu tố /api — code sẽ tự thêm
#
# Dev (backend chạy local):
# VITE_API_BASE_URL=http://localhost:5278
#
# Production (Railway):
VITE_API_BASE_URL=https://glowauraapimongodb-production.up.railway.app/api
